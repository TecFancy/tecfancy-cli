{
  "name": "tecfancy-cli",
  "private": true,
  "workspaces": [
    "core/*",
    "commands/*",
    "models/*",
    "utils/*"
  ],
  "scripts": {
    "build": "lerna exec --parallel -- tsc",
    "build:watch": "lerna exec --parallel -- tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "lerna": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": "^16.20.2"
  }
}
