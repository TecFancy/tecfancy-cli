{
  "name": "tecfancy-cli",
  "private": true,
  "workspaces": [
    "core/*",
    "commands/*",
    "models/*",
    "utils/*"
  ],
  "scripts": {
    "build": "lerna exec --parallel -- tsc",
    "build:watch": "lerna exec --parallel -- tsc -w",
    "ncu": "ncu"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "lerna": "^8.0.2",
    "npm-check-updates": "^16.14.14",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=16.20.2"
  }
}
